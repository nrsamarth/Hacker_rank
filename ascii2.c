#include <stdio.h>

int main() {
    int asciiValue;
    
    // Read an integer (ASCII value) from input
    scanf("%d", &asciiValue);
    
    // Print the corresponding character
    printf("%c\n", asciiValue);

    return 0;
}
